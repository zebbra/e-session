mutation postMessage($room: String!, $author: String!, $text: String!) {
  say(text: $text, author: $author, room: $room) {
    text
    author {
      id
      name
      handRaised
      conferenceJoined
      screenShared
      jid
      role
    }
  }
}
