# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Room {
  id: String!
  name: String!
  messages: [Message!]!
  users: [User!]!
}

type Message {
  room: Room!
  text: String!
}

type User {
  id: String!
  name: String!
  room: Room
}

type Query {
  user(id: String!): User
  room(name: String!): Room!
}

type Mutation {
  login(name: String!): User!
  logout(id: String!): User!
  join(roomId: String!, userId: String!): User!
  leave(roomId: String!, userId: String!): User!
  say(text: String!, room: String!): Message!
}

type Subscription {
  roomJoined(roomId: String!, userId: String!): User!
  roomLeft(roomId: String!, userId: String!): User!
  userConnected(userId: String!, roomId: String!): User!
  messagePosted(room: String!): Message!
}
